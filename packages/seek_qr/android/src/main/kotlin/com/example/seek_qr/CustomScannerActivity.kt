package com.example.seek_qr

import com.journeyapps.barcodescanner.CaptureActivity

/**
 * CustomScannerActivity is a dedicated activity for scanning QR codes.
 * It uses a custom layout and inherits from CaptureActivity provided by the zxing library.
 */
class CustomScannerActivity : CaptureActivity() {}